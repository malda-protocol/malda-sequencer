[profile.default]
  ffi = true
  fs_permissions = [{ access = "read-write", path = "./" }]
  libs = ["lib"]
  out = "out"
  src = "contracts"
  test = "tests"

  # This test is not run because we need real proover 
  no_match_test = "test_Set_WhenLiquidityIsZero_Reverts"

  # See more config options https://github.com/foundry-rs/foundry/tree/master/config
